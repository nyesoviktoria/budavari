import { ImageItemPipe } from './image-item.pipe';

describe('ImageItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageItemPipe();
    expect(pipe).toBeTruthy();
  });
});
