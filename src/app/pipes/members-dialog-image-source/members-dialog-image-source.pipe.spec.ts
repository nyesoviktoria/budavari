import { MembersDialogImageSourcePipe } from './members-dialog-image-source.pipe';

describe('MembersDialogImageSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new MembersDialogImageSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
