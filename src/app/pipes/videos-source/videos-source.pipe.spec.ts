import { VideosSourcePipe } from './videos-source.pipe';

describe('VideosSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new VideosSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
