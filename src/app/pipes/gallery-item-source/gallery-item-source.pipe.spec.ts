import { GalleryItemSourcePipe } from './gallery-item-source.pipe';

describe('GalleryItemSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GalleryItemSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
