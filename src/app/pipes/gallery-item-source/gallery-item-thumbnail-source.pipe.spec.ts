import { GalleryItemSmallSourcePipe } from './gallery-item-thumbnail-source.pipe';

describe('GalleryItemSmallSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GalleryItemSmallSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
