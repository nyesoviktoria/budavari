import { RecordsAudioSourcePipe } from './records-audio-source.pipe';

describe('RecordsAudioSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new RecordsAudioSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
